function [bound] = createBound(bound,strict)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here
    bound.bound = bound;
    bound.strict = strict;
    bound.unbound = 0;

end

